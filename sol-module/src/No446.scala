/**
  * Created by lilisun on 3/1/19.
  */
object No446 {
  // A不是等差=> 找到最长等差数列
  // 数组 A 包含 N 个数，且索引从 0 开始。该数组子序列将划分为整数序列 (P0, P1, ..., Pk)，P 与 Q 是整数且满足 0 ≤ P0 < P1 < ... < Pk < N。

  // dx0 , dx1, ... , dxk
  // find p, 使得相邻dx 和为 p, 求p的最小值
  def numberOfArithmeticSlices(A: Array[Int]): Int = ???

  def maxArithSlices(A: Array[Int]): Int =  ???
  // A是等差
  def numberOfArithmetic(A: Array[Int]): Int = ???


}
